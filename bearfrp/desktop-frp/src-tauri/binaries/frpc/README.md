# frpc binaries

Put platform-specific frpc binaries here before packaging:

- `darwin-arm64/frpc`
- `darwin-amd64/frpc`
- `linux-amd64/frpc`
- `linux-arm64/frpc`
- `windows-amd64/frpc.exe`

Development builds also try the repository root `frpc` binary as a fallback.
