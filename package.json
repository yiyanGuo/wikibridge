{
  "name": "wikibridge-owned-formatting",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "format:owned": "node scripts/format-owned.mjs",
    "format:owned:check": "node scripts/format-owned.mjs --check",
    "test:e2e:public-frps": "npm --prefix desktop run test:e2e:public-frps"
  },
  "devDependencies": {
    "prettier": "3.8.5"
  }
}
